# 无连续三位之和超过 9 的 20 位数

## DP 状态设计

设 $dp[pos][d_1][d_2]$ 表示已填 $pos$ 位、末两位分别为 $d_1, d_2$ 时的合法方案数。

转移条件：新数字 $d$ 满足 $d_1 + d_2 + d \leq 9$。

## 边界

首位 $d_1 \in [1, 9]$，$d_2 = 0$（虚拟前导位）。

## 复杂度

$O(20 \times 10 \times 10 \times 10) = O(20000)$ 操作。

## 答案

$$344\,454\,791\,613\,368$$