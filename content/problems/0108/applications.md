**并联电阻体现了同样的倒数关系。** 两个理想电阻的等效阻值满足 $1/R=1/R_1+1/R_2$。若把阻值限制为某单位的正整数倍，寻找指定等效阻值的组合就能化成因子配对；但实际选型还要考虑标准阻值、容差与功率，不能直接把本题的解数当成可采购组合数。

**因子分解也用于整数尺寸的布局枚举。** 将固定数量的单元排成矩形，就是枚举面积的因子对；不区分旋转方向时，需要像本题一样把对称配对合并，并单独处理正方形。先证明计数对象之间的一一对应，再利用乘法结构计数，可以避免逐个生成全部布局。这里可迁移的是代数化简与消除重复的方法，而不是题目的数值答案。
