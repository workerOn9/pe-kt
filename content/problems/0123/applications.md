**模幂让大指数计算保持在有限数值范围内。** RSA 与有限域 Diffie–Hellman 使用模幂运算，平方—乘法把指数展开成二进制，在每一步乘法后取模，无需保存完整整数幂。本题的对照解用同样的计算方式；实际密码实现还必须处理侧信道，不能直接照搬教学代码。

**代数化简与通用实现适合交叉验证。** 专用公式可以快得多，通用模幂则直接对应定义。让两条路径在样例、边界与完整输入上相互核对，有助于发现漏取模、序号偏移和阈值方向写反等错误。

素数筛适用于需要反复访问小素数的批量计算，例如整数分解中的试除阶段；一次建立表后可复用。这里把建表成本计入基线，避免只报告搜索循环而低估实际耗时。
