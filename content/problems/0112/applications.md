**前缀状态能代表整片搜索空间。** 解析器和有限状态机逐字符读入数据，只保存与后续判断有关的状态；本题只需记录是否见过上升、下降，一旦两者都出现，继续追加数字便不可能恢复单调。这与模式匹配中进入不可逆接受状态的处理方式相同。

**区间聚合可以替代逐条扫描。** 数据统计和查询执行常利用一个分组内性质不变的事实，直接累计整组贡献。本题的弹跳前缀对应连续整数区间，区间内部非弹跳数计数不变，于是能用一次整数方程定位目标比例。可迁移的是“证明整组同质后再跳过”的方法，而不是十进制数字是否弹跳这一具体性质。
