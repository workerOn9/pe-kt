# 数字根因子和

定义 $mdr(n)$ 为 $n$ 的各位数字之积，反复应用直到成为一位数，这个一位数记为 $mdrs(n)$。

对任意整数 $n$，设其质因数分解为 $n = p_1 \cdot p_2 \cdots p_k$（按重数计）。对每个 $p_i$ 计算 $mdrs(p_i)$。

求所有满足 $mdrs(n) = mdrs(p_1) + mdrs(p_2) + mdrs(p_3)$ 的正整数 $n \le 250000$ 之和。