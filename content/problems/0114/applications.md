**带约束的二进制序列可以用状态计数。** 在存储与通信编码中，常需限制连续相同符号的长度或相邻事件的间隔，以满足同步、检测等要求。实际约束不一定与本题相同，但方法一致：把已经出现的短序列按末尾状态分类，逐位延长并累加合法路径数，再用总数评估可承载的信息量。

**隔板法提供另一种核算视角。** 如果对象可以拆成若干连续区段和间隔，先扣除每段的最低长度，再分配剩余长度，能把布局枚举变成非负整数解计数。递推适合逐步增加总长度，组合式适合固定区段数；让两种模型相互校验，比用同一递推写两遍更容易发现边界漏计。
