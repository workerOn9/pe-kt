Calkin–Wilf 树与 Stern–Brocot 树是「用整数索引枚举全部分数」的经典结构，本题展示的是它的双向使用：既可以从分数读出路径，也可以从路径还原最短的整数表示。这类结构在工程里对应分数频率合成器的分频比求解、以及任意精度有理数运算中的连分数化——把浮点或分数转换成分母最小的近似分数，靠的就是同一种树上下降。它还被用于无损压缩与算术编码：当符号概率是分数时，编码路径可以直接用树上的比特串表示，游程越少、压缩比越高，这与本题把二进制写成「几个 1、几个 0」的分段形式是同一件事。