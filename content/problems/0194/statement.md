考虑由两种单元构成的图形：单元 $A$（三角形）和单元 $B$（四边形）。将多个单元沿竖直边依次拼接，得到一条链状图。

一种 $(a, b, c)$ 型配置由 $a$ 个 $A$ 单元和 $b$ 个 $B$ 单元组成，用不超过 $c$ 种颜色对图的每个顶点染色，使得相邻顶点颜色不同。

令 $N(a, b, c)$ 为 $(a, b, c)$ 型配置的染色方案数。例如 $N(1, 0, 3) = 24$、$N(0, 2, 4) = 92928$、$N(2, 2, 3) = 20736$。

求 $N(25, 75, 1984)$ 的最后 $8$ 位数字。
