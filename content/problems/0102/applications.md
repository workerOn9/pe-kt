**有向面积是计算几何的基本判据。** 地图中的点定位、二维网格单元归属和三角形碰撞检测，都需要判断点位于边的哪一侧。把问题化为行列式符号，可以避免计算角度和斜率，也自然处理竖直边。

**重心坐标还承担插值工作。** 在三角形网格上，顶点的颜色、温度或位移可以按三个重心权重插值；有限元计算也使用与此相关的局部形函数。本题只需判断权重是否严格为正，真正的插值则需要算出权重数值。

工程实现必须明确边界归属：相邻单元共享一条边时，应避免重复计数或漏掉边界点。整数坐标能用精确行列式；面对浮点测量数据，则需另行设计可靠的几何谓词，不能把本题的整数比较直接当成通用精度保证。
